export const metadata = {
  title: "Flashcards | Best Vocabulary",
  description: "Learn vocabulary with interactive flashcards",
};

export default function FlashcardsLayout({ children }) {
  return <>{children}</>;
}
