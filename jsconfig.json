{
  "compilerOptions": {
    "baseUrl": "./", // Base directory to resolve non-relative module names
    "paths": {
      "@/*": ["./*"] // Alias for import paths starting with "@/": resolve to "./"
    }
  }
}
